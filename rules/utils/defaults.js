
const defaultInitializers = [
  'Array.from',
  'Array.fromAsync',
  'Array.of',
  'Map.groupBy',
  'Object.create',
  'Object.entries',
  'Object.fromEntries',
  'Object.getOwnPropertyNames',
  'Object.getOwnPropertySymbols',
  'Object.groupBy',
  'Object.keys',
  'Object.values',
  'structuredClone',
];

const defaultReducers = ['reduce'];

module.exports = {defaultReducers, defaultInitializers};
